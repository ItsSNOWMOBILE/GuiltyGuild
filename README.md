
  # Shadow Slave UI Design

  This is a code bundle for Shadow Slave UI Design. The original project is available at https://www.figma.com/design/5UQP8xDxZ9YS1RAb0ZLWny/Shadow-Slave-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  